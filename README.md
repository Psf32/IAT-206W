# IAT-206W
This is for the final draft #3 submission. The canvas submission only takes .docx files however when I try to export the file from word, it autoformats itself weirdly.
It dosen't have double spacing or page numbers or even page separation.
When I export as a pdf it formats fine so idk?
In a text editor it fixes itself so if you import it back into word it should be in proper format.
